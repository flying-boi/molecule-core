Frontend stub. Use Create React App or Vite to initialize a frontend app.

Suggested quick start:

```bash
cd frontend
npx create-react-app .
npm install axios
npm start
```

Add a simple UI to list proposals and create proposals interacting with backend.